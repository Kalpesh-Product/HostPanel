// Savor uses the shared template context; these aliases keep its own files unchanged.
export {
  TplProvider as SavorProvider,
  useTpl as useSavor,
  type TplCtx as SavorCtx,
} from "../shared/TplContext";
